{
  "name": "mosaic",
  "version": "1.0.0",
  "description": "Image storage and album organizer — CSE 4500 Final Project",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "seed": "node seed.js",
    "seed:reset": "node seed.js --reset",
    "test": "echo \"No automated tests — see DEMO_SCRIPT.md for the manual test plan.\" && exit 0"
  },
  "dependencies": {
    "better-sqlite3": "^9.4.3",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^2.0.0"
  }
}
